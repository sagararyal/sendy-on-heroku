<?php
	// "Files" button suppressed. The TinyFileManager backend doesn't list
	// objects from S3/R2 cleanly, so we hide the entry point. Uploads still
	// work via the inline image upload in the campaign editor (GrapesJS /
	// CKEditor) and land in the configured bucket. To re-enable, restore
	// this file from v7 stock and re-enable includes/filemanager/sendy-config.php.
?>
